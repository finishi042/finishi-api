<div class="email-body">
  <p class="greeting">{{#if name}}Hey {{name}}!{{else}}Hey there!{{/if}}</p>

  <p>{{message}}</p>

  {{#if ctaLabel}}
  <div class="cta-wrap">
    <a class="cta" href="{{ctaUrl}}">{{ctaLabel}}</a>
  </div>
  {{/if}}
</div>
