<div class="body">
  <p>{{#if name}}Hi {{name}},{{else}}Hi there,{{/if}}</p>

  <p>{{message}}</p>

  {{#if ctaLabel}}
  <div class="cta-wrap">
    <a class="cta" href="{{ctaUrl}}">{{ctaLabel}}</a>
  </div>
  {{/if}}
</div>
